# --- mosquitto.conf ---
# Listen on the secure port 8883
listener 8883

# Allow clients to connect
allow_anonymous true

# Point to the PQC certificate and key (they are in the same folder)
certfile broker_cert.pem
keyfile broker_key.pem
